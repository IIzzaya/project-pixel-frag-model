/**
 * Deployment base path — the site is served from
 * https://iizzaya.github.io/project-pixel-frag-model/ on GitHub Pages.
 *
 * vinext's `basePath` config rewrites framework URLs (`/_next/...`, Link,
 * Router) but NOT plain references to files in `public/` (and not the
 * `metadata.icons` URLs either). Every public asset referenced from JSX or
 * metadata must therefore be prefixed with this constant.
 */
export const BASE_PATH = '/project-pixel-frag-model';
